# ftp21-feed (gh-pages)

Questo branch è generato automaticamente dal workflow `add-package.yml` sul
branch `main`. Non modificarlo a mano: viene sovrascritto ad ogni release.

Vedi il branch `main` per il funzionamento del feed.
